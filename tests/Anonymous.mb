type Operation func(int, int) -> int

var mul = (a, b) -> a * b

struct Point {
    x: int
    y: int
}

func invoke(a, b, op: Operation) {
    var result = op(a, b)
    print(result)
}

invoke(1, 2, func(a, b) {
    return a + b
})

func invoke(point: Point) {
    invoke(...point, (a, b) -> a / b)
}

invoke((a, b) -> a + b)
invoke(1, 2, mul)
invoke({x: 1, y: 2})
