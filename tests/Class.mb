trait Bar {
    id int

    func construct(id int) {
        self.id = id
    }

    func getId() int {
        return id
    }
}

class Foo {
    use Bar

    func construct(id int) {
        Bar(id)
    }
}

var x = Foo(5)
var id = x.getId()

print(id) # 5
