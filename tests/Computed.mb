class Timer {
    seconds float

    construct(n float) {
        seconds = n
    }

    get minutes() float {
        return seconds / 60
    }

    set minutes(n float) {
        seconds = n * 60
    }
}
