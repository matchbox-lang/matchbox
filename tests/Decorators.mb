type Operation = func(int) -> int

func fn(op: Operation, n) {
    var result = op(n)
    print("Result {result}")

    return result
}

func log(op: Operation): Operation {
    return bind(fn, op)
}

@log
func square(n) {
    return n * n
}
