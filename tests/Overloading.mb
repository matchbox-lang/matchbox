class Point {
    x: int
    y: int

    init(x: int, y: int) {
        self.x = x
        self.y = y
    }

    init(x: int) {
        self(x, x)
    }

    func +=(p: Point) {
        x += p.x
        y += p.y

        return self
    }
}
