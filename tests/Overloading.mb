class Point {
    x: int
    y: int

    construct(x: int, y: int) {
        self.x = x
        self.y = y
    }

    construct(x: int) {
        self(x, x)
    }

    operator +=(v): Point {
        x += v.x
        y += v.y

        return self
    }
}
