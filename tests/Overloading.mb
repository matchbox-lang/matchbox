class Point {
    x: int
    y: int

    func init(x: int) {
        self(x, x)
    }

    func clone() -> Point {
        return Point(x, y)
    }

    func drop() {
        print(x, y)
    }

    func += (p: Point) {
        x += p.x
        y += p.y
    }
}
