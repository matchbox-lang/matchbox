trait Bar {
    id: int

    func getId(): int {
        return id
    }
}

class Foo {
    use Bar
    
    func setId(id: int) {
        self.id = id
    }
}

var x = Foo(5)
var id = x.getId()

print(id) # 5
