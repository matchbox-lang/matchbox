type Operation = (i32) -> i32

func fn(op: Operation, n) {
    let result = op(n)
    print("Result {result}")

    return result
}

func log(op: Operation): Operation {
    return bind(fn, op)
}

@log
func square(n) {
    return n * n
}
