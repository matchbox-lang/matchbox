class Point {
    x: i32
    y: i32

    func init(x: i32) {
        self(x, x)
    }

    func clone() -> Point {
        return Point(x, y)
    }

    func drop() {
        print(x, y)
    }

    func += (p: Point) {
        x += p.x
        y += p.y
    }
}
