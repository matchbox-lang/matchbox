let x: i8 = 100
let y = 10.5

let i: i32 = x # Implicit OK

let i: i32 = y # Implicit Error

let i = i32(y) # Explicit OK

let i = char(y) # Explicit Error

let i: i64 = i32(y) # Explicit OK > Implicit OK

let s: string = 'c' # Implicit OK

let d: Derived = Derived()

let b: Base = d # Implicit OK
